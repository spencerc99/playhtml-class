export const meta = {
  title: 'Custom Events',
};

## Activities

- Crit and discussion in small groups:
  - Share prototypes
  - Discuss readings
- Demo / lecture:
  - Creating custom events
  - Permissions
  - Cross-site interactions
- Mini challenge
- Debugging and Q+A

## Assignment / Readings

- Finalize your site.
- Optional: implement one custom event or shared behavior that interacts with others' sites.
- Read: `TKTKTKTKT`

## Examples

### Example 1: listen for a shared custom event

So far our data has lived _on_ an element. Sometimes you instead want a one-shot signal that fires for everyone on the page at once — confetti, a sound, a flash — without storing anything. That's a custom event. You register a listener for an event type, and playhtml runs your `onEvent` whenever _anyone_ dispatches it:

```html
<script type="module">
  import { playhtml } from 'https://unpkg.com/playhtml';

  playhtml.init();

  playhtml.registerPlayEventListener('confetti', {
    onEvent: () => {
      console.log('confetti for everyone');
      // ...drop confetti, play a sound, flash the screen, etc.
    },
  });
</script>
```

### Example 2: connect it to a button

Dispatching the event is what triggers everyone's listener. Here we fire it when this visitor clicks a button:

```html
<button id="celebrate-btn">Celebrate</button>
<script type="module">
  import { playhtml } from 'https://unpkg.com/playhtml';

  playhtml.init();

  document.getElementById('celebrate-btn').addEventListener('click', () => {
    playhtml.dispatchPlayEvent({ type: 'confetti' });
  });
</script>
```

The click happens on one person's screen, but because `dispatchPlayEvent` sends the event through playhtml's shared infrastructure, the `confetti` listener from Example 1 runs on _every_ connected page. You can also pass data along with the event (`dispatchPlayEvent({ type: 'confetti', eventPayload: { color: 'gold' } })`) and read it in `onEvent`.

## Submit Your Assignment

Paste your site URL below to submit.

## Live Crit

Submitted projects will appear here for live review.
