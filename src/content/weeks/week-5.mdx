export const meta = {
  title: 'Party!',
  chairs: true,
};

<img src="/persian-rug.png" width="100%" />

## Activities

- Crit and feedback (1hr):
  - Introduce our guest crit!
- What's a web ring? (45 mins):
  - Create a web ring together
  - Create a shared space
- What's next? (45 mins):
  - Physical Showcase (5 mins)
  - Reflection Form (15 mins)
  - QA & Other Resources (20 mins)
- Goodbye & Group photo (5 mins)

## Showcase

We will be showcasing your final projects in a live crit session. Please submit your project URL in the "showcase" tab to share your work with the class.

Each person will have about 1 minute to present their project and 2 minutes to receive feedback from the class and our guest crit. Please be prepared to share your screen and walk us through your project.

### Guest Crits

Monday: [Xin Xin](https://xin-xin.info) is an artist currently making socially-engaged software that explores the possibilities of reshaping language and power relations. As an organizer, Xin co-founded voidLab, a LA-based intersectional feminist collective dedicated to women, trans, and queer folks. They were the Director for Processing Community Day 2019 and they serve on the Processing Foundation Board.

- [Together Net](https://xin-xin.info/projects/2021/02/15/togethernet.html)
- [Consentful Protocol](https://xin-xin.info/projects/2021/02/15/consentfulProtocol.html)

Tuesday: [Nolen]

## What's a web ring?

What is a [web ring](https://en.wikipedia.org/wiki/Webring)? A web ring is a collection of websites linked together in a circular structure. Each site points to another site, creating a continuous loop.

![A diagram of a web ring](/images/Webringwork.png)

When the **World Wide Web** was young, it was conceived as a distributed system for connecting information across different computers. [Tim Berners-Lee’s 1989 proposal](https://www.w3.org/History/1989/proposal.html) addressed a problem at CERN: documents, projects, and institutional knowledge were scattered across incompatible systems. Hyperlinks allowed one addressable resource to point to another without placing everything in a central database.

[![A recreation of the first website in an early line-mode browser](https://home.cern/wp-content/uploads/2026/05/Screenshot-World-Wide-Web-project-small-2-1024x619.png)](https://info.cern.ch/)

This is why some servers were designed to return `index.html` when someone visits a directory, and if no index file exsits, the server may generate a directory listing. For example, if you visit `https://example.com/public/`, the server may return `https://example.com/public/index.html` if it exists. If not, it may return a list of files in that directory:

```text
Index of /notes/

week-01.html
week-02.html
reading-list.pdf
```

One of the most important aspects of the early Web was the [hyperlink](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a), which made connecting and linking resources possiblebut. However, it did not guarantee visibility. Someone had to discover a page, decide it mattered, and link to it. Therefore, many websites were isolated and difficult to find.

[Yahoo emerged in 1994.](https://web.stanford.edu/class/e145/2007_fall/materials/Yahoo_1995_STVPCase.pdf) It was initially a human-curated directory rather than a modern crawler-based search engine.

[![Yahoo in 1994, organized as a hierarchy of categories](/images/yahoo-1994.png)](https://www.webdesignmuseum.org/gallery/yahoo-1994)

Yahoo presented the Web as categories such as Art, Business, Computers, and Politics. [View the archived interface.](https://www.webdesignmuseum.org/gallery/yahoo-1994)

Directories and search engines solved discovery by creating centralized maps of the Web. This made navigation easier, but also concentrated power: directories decided what belonged in each category, while search engines decided what appeared first.

A **web ring** instead, linked independently operated sites around a shared subject. [An early example, EUROPa, appeared in 1994; Sage Weil later developed a centrally managed system that made rings easier to maintain.](https://crln.acrl.org/index.php/crlnews/article/view/22105/28059) Participating sites usually included navigation like this:

```html
<nav>
  <a href="/previous">← Previous</a>
  <a href="/members">All sites</a>
  <a href="/next">Next →</a>
</nav>
```

Unlike a search engine, a web ring made curation explicit. Joining a web ring means meeting neighboring sites, sharing visitors, and presenting oneself as part of a community. Rings were not completely decentralized: a ringmaster could control membership, and abandoned sites could break navigation. But still they offered a different politics of discovery sites.

A contemporary example is the [XXIIVV Webring](https://github.com/xxiivv/webring), which connects artists’ and developers’ hand-built personal websites.

## Let's create a web ring together!

The easiest way to create a web ring is to have a page that contains links to all the other sites in the ring. Each site in the ring would have a link back to the main page, creating a circular structure.

However, this is not as visually interesting or dynamic as we would like it to be, as they are completely ad-hoc and static. If we wish to create a more dynamic and visually interesting web ring, we can instead create a database of all the sites in the ring, and then use that database to generate a dynamic plugin that displays all the sites in the ring. This way, we can easily add or remove sites from the ring, and the page will automatically update to reflect those changes, by using playhtml.

1. We will first need to add those sites to our database (the data is stored in playhtml's database).

2. Then we can use the `class-webring.js` script to display the sites in the ring on our page. The script will automatically generate a dynamic plugin that displays all the sites in the ring, and it will update automatically when new sites are added or removed.

```html
<script
  type="module"
  src="https://class.playhtml.fun/class-webring.js"
></script>
```

3. Each site is displayed with its favicon. The embed first looks for every icon declared in the site's `<link rel="icon">` tags. It then tries conventional `.ico`, `.png`, `.jpg`, `.jpeg`, `.gif`, `.svg`, and Apple touch-icon paths, followed by a favicon cache, the submitted circle image, and finally the project's emoji.

### What's a favicon?

A **favicon** is a small icon that represents a website. It is typically displayed in the browser's address bar, next to the page title in tabs, and in bookmarks. Favicons help users quickly identify and navigate to their favorite websites.

In our web ring, we use favicons to represent each site. A site can declare the exact icon it wants:

```html
<link rel="icon" href="/images/my-tiny-chair.png" type="image/png" />
```

The script will prefer that declared URL whenever it can read the site's HTML.

There is one browser limitation: JavaScript may display a cross-origin image, but it may not read a cross-origin HTML page unless that page opts into CORS. When CORS blocks the HTML request, the static embed cannot inspect its `<link>` tags. That is why the conventional filenames and favicon cache remain necessary fallbacks. Reading every declaration regardless of CORS would require a server-side favicon resolver or proxy.

You can use ICO, PNG, JPG, GIF, or SVG. Declaring the icon in your HTML is more reliable than depending only on a conventional filename:

```html
<link rel="icon" href="/favicon.ico" type="image/x-icon" />
```

## What's Next?

This is the last week of class! We want to make sure you have time to reflect on your experience and share some thoughts with us. Before we say goodbye, there are a couple of things we would like to share with you:

### Physical Showcase

SFPC is organizing a public summer showcase at the National Academy of Design in Midtown Manhattan from **August 20–22**, with a public reception on **August 21**. This is an opportunity to present work developed in our class alongside projects from other SFPC courses.

The showcase can include websites, interactive projects, videos, writing, images, performances, printed matter, or physical objects. SFPC will provide our class with a display desk, and a limited number of Windows computer terminals and shared projection equipment are also available. **Digital work and project information are due August 3.** Physical work can be dropped off on **August 18, from 10am–6pm**, and must be picked up on **August 24 or 25**.

Please let us know soon if you would like to participate, especially if your project involves a performance, sound, projection, unusual installation, or other technical requirements.

### Reflection Form

We will dedicate 15 minutes of our class time to collecting feedback from students, please take a moment to fill out the [reflection form](https://orienting.sfpc.study/is-where-we/reflect-together/final-reflections.html) now. Your feedback is important to us and will help us improve the class for future students.

### QA & Other Resources

Here are some resources that may be helpful as you continue to develop your projects:

Anything else you want to share with the class?

## Let's also create a shared space!

Another way we can make the web ring more dynamic is to create a shared space where we can all interact with each other. This can be done by creating a shared element on each site that is connected to the same state. This way, when one person interacts with their element, it will update the state for everyone else in the ring.

PlayHTML [shared elements](https://playhtml.fun/docs/advanced/shared-elements/) have:

- a **source** on the participant's site
- a **consumer** in the class Showcase
- the same PlayHTML capability on both sides

They share state, but they do not need the same HTML, image, CSS, or layout.

The documentation can be reduced to three rules:

1. A source has a stable `id`, `shared`, and one or more PlayHTML capabilities.
2. A consumer points to `domain/optional-path#elementId` with `data-source`.
3. Every capability you want to share must exist on both source and consumer. Their tags, children, classes, and styling may be completely different.

Here is the basic example from the PlayHTML docs:

```html
<!-- Source on thissite.com -->
<div id="couch" shared can-move>🛋️</div>

<!-- Consumer on anothersite.com -->
<div data-source="thissite.com#couch" can-move>🪑</div>
```

The source is a couch and the consumer is a chair. Their markup looks different, but their `can-move` position is shared.

Our ring follows the same pattern:

```html
<!-- Source on the participant's site -->
<img id="my-ring-charm" shared can-toggle src="/lamp.png" alt="A lamp" />

<!-- Consumer created safely by the class Showcase -->
<img
  data-source="student.example#my-ring-charm"
  can-toggle
  src="https://student.example/circle.png"
  alt=""
/>
```

The **circle image submitted in the form is the consumer element**. The participant keeps complete control over the source element and code on their own site.

### 1. Match the source capabilities

The capability is the kind of state shared between the two sites. It is not the visual style.

| Capability   | Shared state        | Participant can customize                          |
| ------------ | ------------------- | -------------------------------------------------- |
| `can-toggle` | on or off           | colors, images, visibility, sound, local animation |
| `can-spin`   | rotation            | what spins and what the rotation means             |
| `can-grow`   | scale               | what grows and how the surrounding page responds   |
| `can-hover`  | live hover presence | what responds when someone hovers either copy      |
| `can-move`   | dragged position    | the object, its appearance, and its surroundings   |

For example, two students can both use `can-toggle`: one might light a lamp while another reveals a secret garden. The shared contract is the same; the effect is not. The form asks for the capability because the Showcase must put that matching attribute on the circle consumer—not to prescribe the participant's design.

PlayHTML itself also supports `can-mirror`, `can-duplicate`, and custom `can-play`. We do not infer those from a submission because they carry DOM structure, duplication targets, or a custom data schema and renderer. Participants may use them freely on their own sites. Bringing them into the Showcase requires a specific, reviewed adapter rather than executing submitted code.

### 2. Add a source to your site

This is the smallest complete toggle example:

```html
<img
  id="my-ring-charm"
  shared
  can-toggle
  src="/lamp.png"
  alt="Turn the lamp on or off"
/>

<style>
  #my-ring-charm {
    filter: grayscale(1);
    transition: 300ms;
  }

  /* PlayHTML adds .toggled when the shared state is on. */
  #my-ring-charm.toggled {
    filter: grayscale(0) drop-shadow(0 0 20px gold);
  }
</style>

<script type="module">
  import { playhtml } from 'https://unpkg.com/playhtml';

  playhtml.init();
</script>
```

To try a different capability, keep the stable ID and replace `can-toggle` with `can-spin`, `can-grow`, `can-hover`, or `can-move`.

You can continue editing the HTML, images, CSS, and scripts on your site after joining the ring. You do not need to resubmit unless you change the source page URL, element ID, or shared capability.

### 3. Publish, then submit

Deploy the project before adding it to the Showcase. The source must exist at a public URL.

In the submission form, add:

1. Your project details and public URL
2. The emoji, glow color, and optional **circle image URL** for the consumer
3. The exact page URL containing your source element
4. The source's stable element ID, such as `my-ring-charm`
5. The same shared capability used by the source

The form generates a source snippet for comparison. It stores configuration, not participant JavaScript.

### 4. Test both directions

1. Open the participant's deployed site in one window.
2. Open the class Showcase in another window.
3. Interact with the source and watch the circle update.
4. Interact with the circle and watch the source update.

If it does not connect, check:

- Is the source deployed at a public URL rather than only available in a private development environment?
- Does the submitted page include the exact path? `student.example/room` and `student.example/` are different PlayHTML rooms.
- Does the stable ID match exactly?
- Do source and consumer use the same capability?
- Does the source include `shared`?
- Does the page initialize PlayHTML once?

### 5. Keep shaping your place

The ring is not a final upload. Participants can:

- keep changing and redeploying all code on their own site
- customize how their shared state is represented locally
- use a different source image from the class circle image
- return to **Your submissions** to edit those details or remove the project from the registry

The Showcase never executes submitted code. This lets each site remain expressive while the class ring stays safe and structurally consistent.

### 6. Put the whole ring on your site

The shared charm connects one element to the Showcase. This separate embed displays the directory of class sites on any participant website:

```html
<script
  type="module"
  src="https://class.playhtml.fun/class-webring.js"
></script>
```

After deployment, the script lives at [`/class-webring.js`](/class-webring.js). It derives the Showcase registry from the domain serving the script and subscribes read-only. The ring starts closed as a small orbit labeled **Benches for the Internet**. Selecting the label opens the fullscreen image field; the close button returns it to the small orbit.

The class app chooses the script URL automatically. During local development it loads `/class-webring.js` from the current development origin. A production build loads the canonical script above from `class.playhtml.fun`. The resolver lives in `src/lib/classWebringScript.ts`, so previewing and deploying do not require hand-editing a URL.

For a separate staging deployment, use that deployment's origin instead:

```html
<script
  type="module"
  src="https://YOUR-PREVIEW-DOMAIN/class-webring.js"
></script>
```

Visit the hidden [`/webring-demo`](/webring-demo) route to see a pretend independent website load this exact script. The demo always uses 30 sample links, so you can inspect a crowded ring without changing the real registry.
